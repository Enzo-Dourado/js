function cliqueBotao() {
    var num = prompt('Digite um valor: ')
    var sucessor = Number(num) + 1
    var antecessor = Number(num) - 1
    alert(`O valor antecessor à ${num} é ${antecessor} e o sucessor é ${sucessor}.`)
    
}
